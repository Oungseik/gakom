{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "auth-server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --bun run with-env ./src/index.ts",
    "with-env": "dotenv -e ../../.env --",
    "check": "biome check --write .",
    "build": "tsdown",
    "check-types": "tsc --noEmit"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./auth": {
      "types": "./src/auth.ts",
      "default": "./src/auth.ts"
    }
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.8",
    "drizzle-seed": "^0.3.1",
    "tsdown": "^0.17.2",
    "typescript": "^5.9.3"
  }
}
