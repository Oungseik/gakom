{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@repo/db",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --bun run with-env bun run ./src/index.ts",
    "with-env": "dotenv -e ../../.env --",
    "check": "biome check --write .",
    "build": "tsdown",
    "check-types": "tsc --noEmit",
    "db:push": "bun --bun run with-env drizzle-kit push",
    "db:studio": "bun --bun run with-env drizzle-kit studio",
    "db:generate": "bun --bun run with-env drizzle-kit generate",
    "db:migrate": "bun run with-env drizzle-kit migrate"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./auth": {
      "types": "./src/auth.ts",
      "default": "./src/auth.ts"
    }
  },
  "dependencies": {
    "@tursodatabase/database": "^0.3.2",
    "dotenv": "^17.2.3",
    "drizzle-orm": "1.0.0-beta.2-58a4521"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "1.0.0-beta.2-58a4521",
    "drizzle-seed": "^0.3.1",
    "tsdown": "^0.17.2",
    "typescript": "^5.9.3"
  }
}
